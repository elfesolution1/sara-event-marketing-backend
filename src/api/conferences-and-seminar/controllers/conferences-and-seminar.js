'use strict';

/**
 * conferences-and-seminar controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::conferences-and-seminar.conferences-and-seminar');
